name: CI

on:
  push:
  pull_request:

jobs:
  macos-tests:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: swift test
      - name: Build app bundle
        run: ./script/build_and_run.sh --bundle
