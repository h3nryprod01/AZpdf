# Báo cáo mutation — 2026-07-31 (PHA 2)

> Pha **ĐO**: áp mutation văn bản lên code sản phẩm, chạy `swift test`, đếm killed/survived.
> KHÔNG sửa code hay test trong pha này — chỉ đo và ghi. Sửa là việc pha sau, sau khi người xem số.

## Kết quả — LƯỢT 2 (phủ đủ 4 thư mục)

- **Kill rate 44 %** — 27 killed / 61 effective, 34 survived, 16 skipped (gãy biên dịch).
- Script: `script/mutation_test.sh` — 4 toán tử, cap **theo từng thư mục** (20/dir), luôn khôi
  phục file và **từ chối chạy nếu scope có thay đổi chưa commit** (xem "An toàn" bên dưới).
- `--self-test` ✓ — áp mutation đỏ đã biết → báo `killed`. Pipeline bắt được, không phải diễn.

### Vì sao có lượt 2

Lượt 1 chỉ cap theo **toán tử**, nên cả 80 candidate rơi hết vào `Core/`: vòng duyệt chạm nó
trước và lấp đầy cap trước khi tới ba thư mục còn lại. Kill rate 49 % khi đó là **của Core**.
Trớ trêu là Core hoá ra được test **tốt nhất** — nên lượt 1 vô tình đo đúng chỗ đẹp nhất rồi
báo đó là số của dự án.

Đếm dòng có thể mutate: `Core` 193 · `Models` 29 · `Services` 148 · **`Stores` 337 (nhiều nhất,
chưa đo dòng nào)**.

### ĐỌC THEO FILE, không phải theo thư mục

Cap theo thư mục sửa được phân bố **giữa** các thư mục, nhưng **trong** mỗi thư mục mẫu vẫn dồn
vào file đứng đầu bảng chữ cái. Nói "Stores 13 %" là lặp lại đúng sai lầm của lượt 1, nên số
dưới đây ghi theo **file**:

| file | killed | survived | skipped | kill rate |
|---|---|---|---|---|
| `Core/DocumentIR.swift` | 11 | 5 | 2 | **68 %** |
| `Services/CertificateSigningService.swift` | 6 | 6 | 6 | 50 % |
| `Models/AnnotationHandles.swift` | 3 | 3 | 1 | 50 % |
| `Models/PDFAnnotation+AZpdf.swift` | 2 | 2 | 0 | 50 % |
| **`Stores/DocumentStore+Annotations.swift`** | **2** | **14** | 4 | **12 %** |

Mẫu mỗi file: `Stores` 20/20 dồn vào một file · `Core` 18/20 vào `DocumentIR.swift` ·
`Services` 18/20 vào `CertificateSigningService.swift` · `Models` trải đều 5 file.

### Phát hiện lớn nhất

**`DocumentStore+Annotations.swift` — 12 %.** 14 trong 16 mutation hiệu lực sống sót: lật hằng
số, đảo boolean, đảo so sánh, phủ định guard đều **không ai bắt**. Đây là logic chỉnh sửa chú
thích — đúng thứ người dùng dùng nhiều nhất và đúng thứ cả đợt làm việc này khởi đầu từ đó
(khung nét đứt, định dạng chữ, chèn hình).

Nói thẳng: file này build sạch, suite xanh, mà gần như mọi thay đổi hành vi đều lọt.

## An toàn của chính script

`restore_all` chạy `git checkout -- Core Models Services Stores` sau **mỗi** mutation — thao tác
đó **xoá mọi sửa đổi chưa commit** trong 4 thư mục ấy. Đo được: thêm một dòng vào
`Core/DocumentIR.swift` rồi chạy đúng lệnh khôi phục → dòng biến mất, không cảnh báo. Script giờ
kiểm điều kiện trước và **từ chối chạy** nếu scope bẩn, kèm hướng dẫn commit/stash.

## Phát hiện chính

Hai nhóm chiếm phần lớn 35 mutation sống sót:

1. **Default value của portable model (`PDFDocumentModel.swift`)** — ~16 mutation lật default
   (`alpha`, `rotation`, `isBold/isItalic/isUnderline`, `opacity`, `deskew/rotatePages`,
   `hasTimestamp/hasValidationInfo`, `isOpen/isReadOnly/isMultiline`, `writingMode`, `color`,
   `fontSize` clamp, `pageBox` origin, `fieldSpec`). Test dựng các type này với đối số tường
   minh hoặc không kiểm default → lật default không ai bắt. **Đây là dạng "xanh nhưng sai" đúng
   nghĩa**: build sạch, suite xanh, mà hành vi mặc định đã đổi.
   → **Cần 1 test default/round-trip** dựng `PDFDocumentModel` (và các sub-type) với default rồi
   assert từng giá trị mặc định — một test nhỏ bắt được cả chục mutation này.
2. **Cờ `keepingCapacity` của `Array.removeAll`** (`PortableDocumentSession.swift:41/67/68`) — đây
   là gợi ý hiệu năng, **không quan sát được qua hành vi**. Survive là **đúng**, không có test
   hành vi nào phải bắt chúng. Báo cáo ghi rõ để pha sau không tốn công viết test vô nghĩa.

Ngoài ra có boundary trong `DocumentIR` (insert index âm, dedup block, thứ tự sort, clamp
point/bounds, bảng 1 hàng) và default cờ session (`canRedo`, `markAsSaved`, `historyLimit`) —
test nên assert các bất biến đó.

## Danh sách 35 mutation sống sót + test đáng lẽ bắt

`keepingCapacity` = không quan sát được (survive đúng, bỏ qua). Còn lại mỗi dòng ghi test đáng lẽ
phải bắt.

| file:line | op | test đáng lẽ bắt |
|---|---|---|
| Core/DocumentIR.swift:44 | cmp | test `insert` page index âm phải bị reject (guard `index >= 0`); flip thành `<= 0` cho phép index âm |
| Core/DocumentIR.swift:161 | cmp | test gộp block trùng `id` phải KHÔNG ghi đè (`where byID[id] == nil`) |
| Core/DocumentIR.swift:165 | cmp | test sắp xếp block theo `origin.y` phải assert đúng thứ tự (comparator `==`→`!=` phá ổn định) |
| Core/DocumentIR.swift:178 | const | test chấp nhận block có `0 < width ≤ 1` (`width > 0`→`> 1`reject nhầm) |
| Core/DocumentIR.swift:378 | cmp | test clamp điểm về biên phải giữ điểm hợp lệ (`point.x >= 0`→`<= 0`) |
| Core/DocumentIR.swift:379 | cmp | test clamp điểm không vượt `pageSize` (`<= pageSize`→`>= pageSize`) |
| Core/DocumentIR.swift:470 | const | test bảng 1 hàng phải được chấp nhận (`rowCount > 0`→`> 1`) |
| Core/DocumentIR.swift:703 | bool | test `encode` mặc định phải ra compact (`prettyPrinted = false`→`true`) — round-trip không bắt được, phải assert format |
| Core/PDFDocumentEngine.swift:37 | guard | test `search` chuỗi không rỗng phải trả kết quả (guard `!isEmpty`→`isEmpty` làm search rỗng luôn) |
| Core/PDFDocumentEngine.swift:40 | guard | test `search` phải khớp text chứa query (`localizedCaseInsensitiveContains` bị phủ định) |
| Core/PortableDocumentSession.swift:11 | bool | test session mới phải assert `canRedo == false` |
| Core/PortableDocumentSession.swift:22 | const | test `historyLimit` phải clamp `≥ 1` (`max(1,…)`) |
| Core/PortableDocumentSession.swift:41 | bool | `keepingCapacity` — KHÔNG quan sát được, survive đúng |
| Core/PortableDocumentSession.swift:65 | bool | test `replace()` mặc định phải `markAsSaved` (`= true`→`false`) |
| Core/PortableDocumentSession.swift:67 | bool | `keepingCapacity` — KHÔNG quan sát được, survive đúng |
| Core/PortableDocumentSession.swift:68 | bool | `keepingCapacity` — KHÔNG quan sát được, survive đúng |
| Core/PDFDocumentModel.swift:41 | const | test `PDFColor` default `alpha == 1` |
| Core/PDFDocumentModel.swift:84 | const | test `PDFPageDescriptor` default `rotation == 0` |
| Core/PDFDocumentModel.swift:127 | const | test default `color` phải là đen (`red == 0`) |
| Core/PDFDocumentModel.swift:129 | bool | test `PDFFontStyle` default `isBold == false` |
| Core/PDFDocumentModel.swift:130 | bool | test default `isItalic == false` |
| Core/PDFDocumentModel.swift:131 | bool | test default `isUnderline == false` |
| Core/PDFDocumentModel.swift:134 | const | test `fontSize` phải clamp `≥ 1` (`max(1,…)`) |
| Core/PDFDocumentModel.swift:166 | const | test default `opacity == 1` |
| Core/PDFDocumentModel.swift:213 | const | test default `writingMode == 0` |
| Core/PDFDocumentModel.swift:297 | bool | test OCR options default `deskew == false` |
| Core/PDFDocumentModel.swift:298 | bool | test default `rotatePages == false` |
| Core/PDFDocumentModel.swift:366 | bool | test default `hasTimestamp == false` |
| Core/PDFDocumentModel.swift:367 | bool | test default `hasValidationInfo == false` |
| Core/PDFDocumentModel.swift:406 | const | test signature `fieldSpec` default (page index `1`, không phải `0`) |
| Core/PDFDocumentModel.swift:453 | bool | test form field default `isOpen == false` |
| Core/PDFDocumentModel.swift:498 | bool | test default `isReadOnly == false` |
| Core/PDFDocumentModel.swift:499 | bool | test default `isMultiline == false` |
| Core/PDFDocumentModel.swift:603 | const | test default `rotation == 0` |
| Core/PDFDocumentModel.swift:608 | const | test `pageBox` default origin `x == 0` |

## Đề xuất cho pha sau (chỉ gợi ý — pha này không sửa)

- 1 test default/round-trip cho `PDFDocumentModel` + sub-types → bắt hàng loạt mutation default.
- Vài test bất biến cho `DocumentIR` (reject index âm, dedup, sort, clamp, bảng 1 hàng).
- Vài test cho `PortableDocumentSession` (`canRedo`/`canUndo` ban đầu, `markAsSaved`, clamp history).
- Test `search` engine protocol trả kết quả đúng (cần mutool — chạy được trên macOS/Linux CI).
- Bỏ qua `keepingCapacity` — không đáng test.
- Chạy lại mutation với cap **mỗi thư mục** để đo `Stores/Models/Services`, không chỉ `Core`.
